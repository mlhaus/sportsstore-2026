{{#if cart.lines}}
    <small class="navbar-text">{{ countCartItems cart }} items</small>
{{else}}
    <small class="navbar-text">(Empty)</small>
{{/if}}
<a href="/cart?returnUrl={{ escapeUrl (navigationUrl) }}" class="btn btn-sm btn-secondary navbar-btn">
    <i class="bi-cart"></i>
</a>