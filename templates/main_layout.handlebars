<!DOCTYPE html>
<html>
<head>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/font/bootstrap-icons.min.css">
</head>
<body>
<div class="bg-dark text-white py-2 px-1">
    <div class="container-flud">
        <div class="row">
            <div class="col align-baseline pt-1"><a href="/" class="link-light link-offset-2 link-underline link-underline-opacity-0 link-underline-opacity-0-hover">SPORTS STORE</a></div>
            <div class="col-auto text-end">
                {{#if show_cart}}
                    {{> cart_summary}}
                {{/if}}
            </div>
        </div>
    </div>

</div>
{{{ body }}}
</body>
</html>
